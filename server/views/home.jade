extends includes/layout
block main-content
    div(class="container")
        div(class='row')
            div(class="col-md-12")
                if user.authorized
                    p= user.name
                p= message